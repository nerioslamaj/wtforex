version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  e2e:
    jobs:
      - cypress/install
      - cypress/run:
          requires:
            - cypress/install
          command-prefix: PERCY_TOKEN=${PERCY_KEY} npx percy exec --
